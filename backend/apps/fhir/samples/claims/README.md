# Placeholder directory for Kenya eClaims IG sample resources.
# When Claim / ClaimResponse / Coverage converters land, add *.json samples here.
# CI runs scripts/validate-fhir-claims-ig.sh (exits 0 when empty).
