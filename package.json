{
  "name": "@dawatrace/workspace",
  "private": true,
  "version": "0.2.0-rc2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "./scripts/validate_workspaces.sh typecheck",
    "build": "./scripts/validate_workspaces.sh build",
    "test": "./scripts/validate_workspaces.sh test",
    "validate": "./scripts/validate_repository.sh --full",
    "validate:fast": "./scripts/validate_repository.sh --fast",
    "validate:full": "./scripts/validate_repository.sh --full"
  },
  "engines": {
    "node": ">=22.13 <23"
  }
}
