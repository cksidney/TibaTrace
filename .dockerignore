.git
.github
.claude
.ruff_cache
.venv
node_modules
artifacts/generated
backend/.pytest_cache
backend/**/__pycache__
backend/**/*.pyc
apps/**/dist
# packages/*/dist is generated by the shared build. Excluding it keeps the
# image build hermetic: the container builds @dawatrace/shared itself rather
# than silently consuming whatever a developer happened to leave on the host.
packages/**/dist
.venv-win
media
staticfiles
.DS_Store
*.sqlite3
*.log
